{
  "mcpServers": {
    "mcp-npm-publish": {
      "type": "stdio",
      "command": "npx",
      "args": ["--yes", "--registry", "https://registry.npmjs.org", "mcp-npm-publish"],
      "env": {
        "NPM_TOKEN": "${NPM_TOKEN}"
      }
    }
  }
}
